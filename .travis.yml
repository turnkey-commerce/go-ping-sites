language: go

gobuild_args: -x -ldflags "-X main.version=0.1.0"

go:
  - 1.4
  - 1.5
  - 1.6
  - tip

install:
  - go get -d ./...
  - go get github.com/erikstmartin/go-testdb

script:
  - go test ./...
